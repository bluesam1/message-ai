/**
 * Placeholder screen for the new chat tab
 * This tab is handled by the tab layout and shows a dialog instead
 */

import { View } from 'react-native';

export default function NewChatTab() {
  // This screen is never actually shown
  // The tab button opens a dialog instead
  return <View style={{ flex: 1 }} />;
}
